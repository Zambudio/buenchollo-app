[pytest]
testpaths = app/tests
filterwarnings =
    ignore::DeprecationWarning:fastapi
    ignore::DeprecationWarning:starlette
markers =
    integration: tests que requieren PostgreSQL real (excluidos del CI con -m "not integration")
