services:
  buenchollo-api:
    build: .
    container_name: buenchollo-api
    ports:
      - "8001:8000"
    env_file:
      - .env
    restart: always
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
