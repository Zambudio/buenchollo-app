# ──────────────────────────────────────────────
# buenchollo-api — dependencias de desarrollo y testing
# Instalar: pip install -r requirements-dev.txt
# ──────────────────────────────────────────────

-r requirements.txt

# Testing
pytest==9.0.3
pytest-asyncio>=0.23
# httpx ya viene pinneado a 0.27.2 desde requirements.txt (supabase 2.9.1
# exige httpx<0.28). El TestClient síncrono de Starlette funciona bien con
# 0.27.x; sólo el AsyncClient nuevo de FastAPI exige >=0.28, que no usamos.
